---
title: NextJS
head:
  - tag: "meta"
    attrs:
        property: "og:title"
        content: "How to deploy a NextJS app with Hostly"
description: "A guide on how to deploy a NextJS app with Hostly."
---

NextJS is a React framework that enables functionality such as server-side rendering and generating static websites.

[Example repository.](https://github.com/khulnasoft/hostly-examples/tree/main/nextjs)

## Server build (NodeJS)

- Set `Build Pack` to `nixpacks`.

## Static build (SPA)

- Set `Build Pack` to `nixpacks`.
- Enable `Is it a static site?`.
- Set `Output Directory` to `out`.
